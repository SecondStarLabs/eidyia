<%= form_with(model: import_table, local: true) do |form| %>
  <% if import_table.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(import_table.errors.count, "error") %> prohibited this import_table from being saved:</h2>

      <ul>
      <% import_table.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :original_path %>
    <%= form.text_field :original_path, class: 'form-control' %>
  </div>

  <div class="form-group">
    <% if import_table.persisted? %>
      <div class="float-right">
        <%= link_to 'Destroy', import_table, method: :delete, class: "text-danger", data: { confirm: 'Are you sure?' } %>
      </div>
    <% end %>

    <%= form.submit class: 'btn btn-primary' %>

    <% if import_table.persisted? %>
      <%= link_to "Cancel", import_table, class: "btn btn-link" %>
    <% else %>
      <%= link_to "Cancel", import_tables_path, class: "btn btn-link" %>
    <% end %>
  </div>
<% end %>


<br>
<%= form_for(@import_table) do |f| %>
  <% if @import_table.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@import_table.errors.count, "error") %> prohibited this import_table from being saved:</h2>

      <ul>
      <% @import_table.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :original_path %><br />
    <%= f.text_field :original_path %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>